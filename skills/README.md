# Skills

Each Skill should use this layout:

```
skill-name/
├── SKILL.md
├── references/
├── scripts/
├── assets/
└── tests/
```

Required: clear trigger, inputs, outputs, safety boundaries, validation command, and ownership. Keep the entry file short and progressively disclose details.
